<?xml version="1.0" encoding="UTF-8"?>
<svg width="800" height="450" viewBox="0 0 800 450" xmlns="http://www.w3.org/2000/svg">
  <!-- Soft Gradient Background -->
  <defs>
    <linearGradient id="grad-security" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#ecfdf5;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#f0fdf4;stop-opacity:1" />
    </linearGradient>
  </defs>

  <!-- Background -->
  <rect width="800" height="450" fill="url(#grad-security)"/>

  <!-- Decorative circles -->
  <circle cx="100" cy="100" r="150" fill="#34d399" opacity="0.07"/>
  <circle cx="700" cy="350" r="120" fill="#6ee7b7" opacity="0.07"/>
  <circle cx="600" cy="100" r="80" fill="#34d399" opacity="0.09"/>

  <!-- Icon (large) -->
  <text x="400" y="180"
        font-family="Arial, sans-serif"
        font-size="100"
        fill="#34d399"
        opacity="0.25"
        text-anchor="middle">🔒</text>

  <!-- Title (big number) -->
  <text x="400" y="260"
        font-family="Arial, sans-serif"
        font-size="80"
        font-weight="bold"
        fill="#059669"
        text-anchor="middle">Secure</text>

  <!-- Subtitle -->
  <text x="400" y="310"
        font-family="Arial, sans-serif"
        font-size="32"
        font-weight="600"
        fill="#10b981"
        text-anchor="middle">AI Security</text>

  <!-- Description -->
  <text x="400" y="350"
        font-family="Arial, sans-serif"
        font-size="20"
        fill="#10b981"
        opacity="0.8"
        text-anchor="middle">Enterprise-grade protection</text>
</svg>
